import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    flexDirectionRow: {
        flexDirection: 'row'
    },
    imageFromGallery: {
        borderRadius: 8,
        height: 100,
        marginEnd: 15,
        width: 100
    }
});