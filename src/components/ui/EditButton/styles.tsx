import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    captionLink: {
        color: '#207CFD',
        fontSize: 14,
        fontWeight: '500',
        lineHeight: 15,
        letterSpacing: -0.26
    },
    scheduleMarginTop: {
        marginTop: 10
    }
});